#include <iostream>
#include <stdlib.h> 
#include <time.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */




int main(int argc, char** argv) 
{
	int arr[4][4];
    srand((unsigned int)time(NULL)); 
                                        //1.对arr进行赋值，不能用scanf从键盘输入值，要进行随机初始化
                                        //2.把arr打印出来
										//3.计算arr的每一行的和
										//4.计算arr的对角线上的和
										//5.计算arr上三角的和 
										 
	return 0;
}
